import styled from 'styled-components';

const ContentOpenMusic = styled.div`
    background: var(--color-principal);
    display: flex;
    justify-content: space-between;
`;

export default ContentOpenMusic;